src_dir: src

src_files:
    - requireAjax.js
    - '[^b]*.js' # skip boot.js so it's at the end
    - boot.js

spec_dir:
    - spec

spec_files:
    - '**/*[sS]pec.js'

helpers:
    - helpers/spec-helper.js

stylesheets:

